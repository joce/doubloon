name: CI

on:
  push:
    branches-ignore:
      - "experimental/*"
  pull_request:
    branches-ignore:
      - "experimental/*"

jobs:
  spell-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v5

      - name: Check spelling
        uses: streetsidesoftware/cspell-action@v7

  ci-checks:
    needs: spell-check
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        poetry-version: ["2.2.1"]
    steps:
      - name: Check out the repository
        uses: actions/checkout@v5

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install poetry ${{ matrix.poetry-version }}
        run: |
          python -m ensurepip
          python -m pip install --upgrade pip
          pip install poetry==${{ matrix.poetry-version }}

      - name: Install tox dependencies
        run: poetry install --only tox --no-root

      - name: Run tox
        run: poetry run tox

  coverage:
    needs: spell-check
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test-type:
          - name: unit
            marker: "not ui and not integration"
          - name: integration
            marker: "integration"
          - name: ui
            marker: "ui"
    steps:
      - name: Check out the repository
        uses: actions/checkout@v5

      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install poetry 2.2.1
        run: |
          python -m ensurepip
          python -m pip install --upgrade pip
          pip install poetry==2.2.1

      - name: Install project with dev dependencies
        run: poetry install --with dev --no-root

      - name: Run ${{ matrix.test-type.name }} tests with coverage
        run: |
          poetry run pytest \
            -m "${{ matrix.test-type.marker }}" \
            --cov=src \
            --cov-report=xml:coverage-${{ matrix.test-type.name }}.xml \
            --junitxml=junit-${{ matrix.test-type.name }}.xml -o junit_family=legacy

      - name: Upload ${{ matrix.test-type.name }} test coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: coverage-${{ matrix.test-type.name }}.xml
          flags: ${{ matrix.test-type.name }}
          verbose: true

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          file: junit-${{ matrix.test-type.name }}.xml
